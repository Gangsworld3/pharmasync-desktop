export const IPC_CONTRACTS = Object.freeze({
  GET_INVENTORY: Object.freeze({
    request: Object.freeze({}),
    response: Object.freeze({
      success: "boolean",
      data: "object"
    })
  })
});
